<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Admin | Contact Messages</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/header.css">
  <link rel="stylesheet" href="/footer.css">
</head>
<body>

  <div class="container mt-5 mb-5">
    <h2 class="text-center mb-4">📬 Contact Messages</h2>
<div class="d-flex justify-content-end mb-3">
  <a href="/home" class="btn btn-outline-primary">
    Return to Dashboard
  </a>
</div>

    <% if (messages && messages.length > 0) { %>
      <div class="table-responsive">
        <table class="table table-bordered mt-4">
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Message</th>
      <th>Date</th>
      <th>Action</th> <!-- New column -->
    </tr>
  </thead>
  <tbody>
    <% messages.forEach(msg => { %>
      <tr>
        <td><%= msg.name %></td>
        <td><%= msg.email %></td>
        <td><%= msg.message %></td>
        <td><%= msg.createdAt.toLocaleString() %></td>
        <td>
          <form action="/admin/messages/<%= msg._id %>?_method=DELETE" method="POST">
            <button class="btn btn-sm btn-danger" onclick="return confirm('Are you sure you want to delete this message?')">
              Delete
            </button>
          </form>
        </td>
      </tr>
    <% }) %>
  </tbody>
</table>

      </div>
    <% } else { %>
      <div class="alert alert-info text-center">No messages received yet.</div>
    <% } %>
  </div>

</body>
</html>
