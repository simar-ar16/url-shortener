<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Verify OTP - Shortify</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
  <style>
    body {
      background: linear-gradient(to right, #667eea, #764ba2);
      font-family: 'Segoe UI', sans-serif;
      color: #fff;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }

    .otp-card {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(6px);
      padding: 30px;
      border-radius: 16px;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.3);
      max-width: 420px;
      width: 100%;
    }

    .otp-card h2 {
      color: #fff;
      margin-bottom: 20px;
      text-align: center;
    }

    .form-control {
      border-radius: 8px;
    }

    .btn-primary {
      background-color: #ffd369;
      border: none;
      color: black;
      font-weight: bold;
    }

    .btn-primary:hover {
      background-color: #f6c244;
    }

    .alert {
      margin-top: 15px;
    }
  </style>
</head>
<body>
  <div class="otp-card">
    <h2>🔐 Verify Your Email</h2>

    <% if (locals.error) { %>
      <div class="alert alert-danger text-center"><%= error %></div>
    <% } %>

    <% if (locals.message) { %>
      <div class="alert alert-success text-center"><%= message %></div>
    <% } %>

    <form action="/user/verify-otp" method="POST">
      <div class="mb-3">
        <label for="otp" class="form-label">Enter the 6-digit OTP sent to your email</label>
        <input type="text" class="form-control" id="otp" name="otp" required placeholder="e.g. 123456" />
      </div>

      <input type="hidden" name="email" value="<%= email %>" />

      <button type="submit" class="btn btn-primary w-100">Verify OTP</button>
    </form>
  </div>
</body>
</html>
